#include<iostream>
  #include<string>
  using namespace std;
  int main()
  {
   int i;
   for(i=1;i<=30;i++)
   {
    cout<<(i%3==0 and i%5==0 ? "FIZZBUZZ":(i%3==0 ? "FIZZ":(i%5==0 ? "BUZZ":to_string(i))));
   }
   return 0;
  }